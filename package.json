{
  "name": "activitystreams",
  "version": "0.0.1",
  "description": "ActivityStrea.ms in mongodb",
  "main": "index.js",
  "scripts": {
    "compile": "./node_modules/.bin/babel --presets es2015 -d lib/ src/",
    "test": "npm run compile && jest",
    "prepublish": "npm run test"
  },
  "devDependencies": {
    "babel-core": "^6.24.1",
    "babel-preset-es2015": "^6.24.1",
    "husky": "^0.13.3",
    "jest-cli": "^20.0.0",
    "lint-staged": "^3.4.0",
    "prettier": "^0.22.0"
  },
  "dependencies": {
    "babel-cli": "^6.24.1",
    "mongodb": "^2.2.25"
  },
  "jest":{
    "verbose": true,
    "collectCoverage": true,
    "testEnvironment": "node"
  }
}
